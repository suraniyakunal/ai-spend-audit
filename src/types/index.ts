/**
 * Barrel export — all shared types from a single import path.
 * Usage: import { AuditInput, AuditResult, ... } from '@/types';
 */
export type {
    PricingPlan,
    PricingCategory,
    PricingTool,
    ToolEntry,
    PrimaryUseCase,
    AuditInput,
    ToolAuditResult,
    SavingsLevel,
    AuditResult,
    LeadInput,
    SummaryResult,
} from '@/lib/types';
